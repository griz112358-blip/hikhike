<view class="index">
  <map
    class="map-container"
    longitude="{{longitude}}"
    latitude="{{latitude}}"
    markers="{{markers}}"
    scale="{{scale}}"
    show-location
    polyline="{{polyline}}"
  />
  <view class="distance-display">
    总距离: {{totalDistance}} 米
  </view>
</view>


